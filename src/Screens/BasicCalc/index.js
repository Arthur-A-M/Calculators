import { Text, View } from 'react-native';

import { styles } from './styles';

export default BasicCalcScreen = () => {
    return (
      <View style={styles.container}>
        <Text>This is BasicCalcScreen</Text>
      </View>
    );
  }